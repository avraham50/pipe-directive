import { PipeTopTenPipe } from './pipe-top-ten.pipe';

describe('PipeTopTenPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTopTenPipe();
    expect(pipe).toBeTruthy();
  });
});
