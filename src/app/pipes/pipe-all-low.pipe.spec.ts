import { PipeAllLowPipe } from './pipe-all-low.pipe';

describe('PipeAllLowPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeAllLowPipe();
    expect(pipe).toBeTruthy();
  });
});
