import { PipeMaxValPipe } from './pipe-max-val.pipe';

describe('PipeMaxValPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMaxValPipe();
    expect(pipe).toBeTruthy();
  });
});
