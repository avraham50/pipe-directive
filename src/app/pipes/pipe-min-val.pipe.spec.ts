import { PipeMinValPipe } from './pipe-min-val.pipe';

describe('PipeMinValPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMinValPipe();
    expect(pipe).toBeTruthy();
  });
});
